<% iam_users.each do |user| -%>
resource "aws_iam_user" "<%= module_name_of(user) %>" {
    name = "<%= user.user_name %>"
    path = "<%= user.path %>"
    tags = {
<% iam_tags_of(user).each do |tag| -%>
      "<%= tag.key%>" = "<%= tag.value %>"
<% end -%>
    }
}

<% end -%>
